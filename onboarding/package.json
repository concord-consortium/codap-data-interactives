{
  "name": "onboarding-plugins",
  "type": "module",
  "devDependencies": {
    "node-fetch": "^3.2.10"
  },
  "scripts": {
    "build-onboarding-pre": "mkdir target",
    "build": "bin/build",
    "clean": "rm -rf target",
    "strings:pull": "node bin/pull-strings.js --out=./source/strings.json"
  }
}
